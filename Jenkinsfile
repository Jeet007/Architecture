pipeline {
    agent any
    
    stages {
        stage ('Clean build') {
            steps{
                script{
                     // command to build
                }
            }
        }
    }
}
